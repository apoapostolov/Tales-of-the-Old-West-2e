<!-- markdownlint-disable MD013 -->

# [Module Name] — [One-line description]

> **STATUS: WORKSHOP MODULE.** A new, engine-agnostic subsystem built from proven primitives. The bulk of this file is the *generic design space* (§2); the worked genre example (§8) is one calibrated instantiation. Drop into any YZE-family game by setting the dials and swapping the nouns.

## Contents

1. Origin — how this was built
2. The generic design space
   - 2a. Mechanism overview
   - 2b. NEW CONCEPTS introduced
   - 2c. Mechanical reference (tables & procedures)
3. The pressure loop
4. Dials
5. Integration points
6. Failure modes & edge cases
7. Validation notes
8. Worked genre example — [Genre]

## 1. Origin — how this was built

- **Source primitive(s):** P# from `16-mechanical-primitives.md` (e.g. P2 capped metacurrency, P4 typed D66, P5 resource die, P6 activity menu, P7 org lifecycle).
- **Reinvention operator:** which of the five from `18 §4` — Domain Transfer / Inversion / Recalibration / Fusion / Abstraction-Climb.
- **Target psychology:** which cell of `17`'s dual-use matrix.
- **What problem it solves:** the design need this subsystem answers.

## 2. The generic design space

### 2a. Mechanism overview

*The abstract procedure, stripped of genre nouns. 2–4 paragraphs naming the resource, the loop, and the core decision. This is the conceptual handle; the runnable content is in 2c.*

### 2b. NEW CONCEPTS introduced

*Every mechanism in this module that **does not exist in the core engine** (`00`–`15`) is flagged here, so a designer adopting the module knows what they are adding to the engine vs. what is recombination of existing primitives. Format:*

- **NEW CONCEPT — [name]:** [one-line definition of what is new and why it extends the engine.]

*Pure recombination of P1–P15 is NOT flagged — only genuine extensions. If a concept is an application of a Reinvention operator (e.g. Inversion of P2), name the operator but flag the **application** as new if no core system does this.*

### 2c. Mechanical reference (tables & procedures)

*The actual runnable content — what makes this module **drop-in** rather than a brainstorming aid. Contains:*

- *Full D66 tables (36 entries, or a tiered family with the build-the-rest guidance explicit — not sample rows).*
- *Step-by-step resolution procedures (numbered).*
- *Die-roll formulas and cost curves (explicit math, not prose).*
- *A designer should be able to run this subsystem from §2c alone, without inventing the tables.*

## 3. The pressure loop

*Which resource is under pressure, what decision the player makes, what consequence follows, what state changes. Name the loop's shape.*

## 4. Dials

*Each tunable parameter, with at least two calibrated settings and the psychology each produces (per `17`).*

## 5. Integration points

*Which engine systems this touches, what it requires to exist, what (if anything) it replaces.*

## 6. Failure modes & edge cases

*Run the module against `13 §5` (exploit taxonomy) and `19 §4` (felt-experience failure modes). Name the specific traps and how to avoid them.*

## 7. Validation notes

*How it passed the pipeline (`13 §8`) and the felt-experience protocol (`19 §7`). Known edge cases and design caveats.*

## 8. Worked genre example — [Genre]

*A concrete instantiation: the dials set, the nouns skinned (per `15`), a brief play example. This is a slice — the genre is illustrative, not load-bearing.*
